package com.company.pages;

import org.openqa.selenium.WebDriver;

public class RedmineUserPage extends RedmineLandingPage{


    //Mapear los elementos web de Administration ( User)

    //Crear los metodos con las acciones a realizar, dar click, crear usuario, etc.

    public RedmineUserPage(WebDriver driver) {
        super(driver);
    }
}
